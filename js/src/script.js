/**
 *
 *	Podcast filter module
 *
 */
(function(win,doc,$) {

	/**
	 * start the application
	 * @return {void}
	 */
	var init = function() {
		console.log('init');
	};

	// initialise the module
	init();


})(window, document, jQuery);